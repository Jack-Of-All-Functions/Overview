const products    = require('./products');
const features    = require('./features');
const styles      = require('./styles');

module.exports = {
  products:   products,
  features:   features,
  styles:     styles
}